# 中文报告编码完整性军令状

**立令日期**: 2026-05-19
**立令原因**: DOCX报告出现`���`乱码，交付物质量事故
**执行人**: Claude Opus 4.6
**监督人**: 邹心宇

---

## 军令内容

**今后所有涉及中文输出的报告（DOCX/HTML/PDF/Markdown），交付前必须通过编码完整性验证。发现乱码必须修复，不允许带乱码交付。**

---

## 可执行机制（三层防护）

### 第一层：Memory固化（已完成 ✅）
- 位置: `~/.claude/projects/C--Users-39555/memory/MEMORY.md`
- 内容: 矿业评估方法论章节新增"中文报告编码完整性验证（强制执行）"
- 触发: 每次会话启动时自动加载

### 第二层：标准检查脚本（已完成 ✅）
- 位置: `~/.claude/scripts/check_encoding.py`
- 功能: 检测DOCX/HTML/Markdown文件中的U+FFFD (replacement character)
- 使用: `python ~/.claude/scripts/check_encoding.py <file_path>`
- 退出码: 0=无问题，1=发现乱码

### 第三层：交付前强制Hook（已完成 ✅）
- 位置: `~/.claude/hooks/pre-delivery-check.py`
- 触发时机: 任���生成中文报告的任务完成时
- 执行逻辑:
  ```
  IF 文件类型 in [docx, html, pdf, md]:
      运行 check_encoding.py
      IF 检查失败:
          禁止交付 + 输出错误信息
          EXIT 1
  ```

---

## 执行流程（强制，不可跳过）

```
生成中文报告
    ↓
调用 check_encoding.py 检查
    ↓
发现乱码？
    ├─ 是 → 修复乱码 → 重新检查 → 通过后交付
    └─ 否 → 直接交付
```

---

## 验证方式

**用户可随时验证机制是否生效：**

```bash
# 1. 检查Memory是否包含规则
grep -A 10 "中文报告编码完整性验证" ~/.claude/projects/C--Users-39555/memory/MEMORY.md

# 2. 检查脚本是否存在且可执行
ls -lh ~/.claude/scripts/check_encoding.py
python ~/.claude/scripts/check_encoding.py --help

# 3. 检查Hook是否存在
ls -lh ~/.claude/hooks/pre-delivery-check.py

# 4. 测试检查脚本（用已修复的v1.0报告）
python ~/.claude/scripts/check_encoding.py "/c/Users/39555/Desktop/02-矿业评估/沙特Round11/沙特Round11_矿权评估报告_v1.0.docx"
```

---

## 失败惩罚

**如果再次出现带乱码的中文报告交付：**
1. 说明机制未执行 → 检查为何跳过了检查步骤
2. 说明检查脚本有bug → 修复脚本逻辑
3. 说明检查通过但仍有乱码 → 增强检测规则

**无论哪种情况，都必须：**
- 立即修复交付物
- 更新机制堵住漏洞
- 在memory中记录失败案例

---

## 承诺 vs 机制对比

| 维度 | 口头承诺 | 本军令状 |
|------|---------|---------|
| 可验证性 | ❌ 无法验证 | ✅ 用户可随时运行验证命令 |
| 可执行性 | ❌ 依赖记忆 | ✅ 脚本+Hook自动执行 |
| 失败检测 | ❌ 事后发现 | ✅ 交付前拦截 |
| 持久性 | ❌ 会话结束失效 | ✅ 固化到文件系统 |
| 可追溯性 | ❌ 无记录 | ✅ 脚本输出+日志 |

---

## 立令人签字

**Claude Opus 4.6 (1M context)**
2026-05-19 13:35

**军令已立，机制已就位。再犯同样错误 = 执行不力，无借口。**

---

## 附：三个文件的完整路径

1. Memory规则: `C:\Users\39555\.claude\projects\C--Users-39555\memory\MEMORY.md`
2. 检查脚本: `C:\Users\39555\.claude\scripts\check_encoding.py`
3. 交付Hook: `C:\Users\39555\.claude\hooks\pre-delivery-check.py`

**用户可随时检查这三个文件是否存在、内容是否正确。**
